import React from 'react';
import {AbsoluteFill} from 'remotion';

// v1 placeholder: in Remotion we will render captions natively later.
// For now, captions can be burned using ffmpeg + ASS.
export const CaptionsAss: React.FC = () => {
  return <AbsoluteFill />;
};
